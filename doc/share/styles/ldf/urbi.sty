\lstdefinelanguage{Urbi}%
{morekeywords=%
  {addgroup,alias,at,bin,binder,class,copy,def,delgroup,disinherits,echo,%
    else,emit,event,every,expression,block,false,flag,flag,identifier,flag,%
    test,flag,time,for,freezeif,from,function,group,identifier,if,in,%
    inherits,loop,new,noop,number,object,onleave,operator,operator,command,%
    return,static,stopif,string,tag,time,timeout,true,unalias,var,%
    var-operator,wait,waituntil,whenever,while%
  },%
  sensitive=true,%
  morecomment=[l]{\#},%
  morestring=[b]{"},%
}[keywords,comments,strings]%
