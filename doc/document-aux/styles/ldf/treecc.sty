%%
%% Language definition of Treecc for listings package.
%%

\lstdefinelanguage{Treecc}%
  {otherkeywords=%
    {\%node,
      \%abstract,\%typedef,
      \%nocreate,
      \%operation,
      \%virtual,\%inline,\%split
      \%option,
      \%enum,
      \%both,\%decls,\%end,
      \%header,
      \%output,
      \%common,
      \%include,\%readonly
    },%
    keywords={\%seeREADME},%
    morestring=[b]{"},%
    sensitive=true
  }[keywords,comments,strings]%
