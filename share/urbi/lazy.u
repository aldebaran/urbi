class Global.Lazy
{
  var code;
  var val;

  function init(value)
  {
    this.code = value |
    this
  };

  function eval()
  {
    val = code.acceptVoid|
    getSlot("val").unAcceptVoid
  };

  function value()
  {
    if (getSlot("val").isNil)
      eval|
    getSlot("val").unAcceptVoid
  };

  function string ()
  {
    getSlot("code").bodyString
  };
};
