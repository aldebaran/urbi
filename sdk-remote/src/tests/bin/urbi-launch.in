#! @SHELL@
# @configure_input@

set -e

case $VERBOSE in
  (x) set -x;;
esac

abs_top_srcdir='@abs_top_srcdir@'
abs_top_builddir='@abs_top_builddir@'
program=$abs_top_builddir/src/urbi-launch
uobject=$abs_top_builddir/src/libuobject/libuobject
exec "$program" -c "$uobject" "$@"

# Local Variables:
# mode: shell-script
# End:
